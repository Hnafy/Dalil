# Same-origin API base. In production Vercel rewrites /api/* to the API (client/vercel.json),
# so the auth cookie stays first-party (required for Safari/ITP). In dev the Vite proxy
# forwards /api/* to the backend (see vite.config.js).
VITE_BASE_API=/api
